function download(name, rows){const csv=[Object.keys(rows[0]||{}).join(','),...rows.map(r=>Object.values(r).join(','))].join('\n'); const a=document.createElement('a'); a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'})); a.download=name; a.click();}
export default function ExportCentar({nalozi,magacin,otpad}){return <div className="grid cards"><div className="card"><h3>Nalozi</h3><button className="primary" onClick={()=>download('nalozi.csv',nalozi)}>Export CSV</button></div><div className="card"><h3>Magacin</h3><button className="primary" onClick={()=>download('magacin.csv',magacin)}>Export CSV</button></div><div className="card"><h3>Otpad</h3><button className="primary" onClick={()=>download('otpad.csv',otpad.length?otpad:[{info:'nema unosa'}])}>Export CSV</button></div></div>}
